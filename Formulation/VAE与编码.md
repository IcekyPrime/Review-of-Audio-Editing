# VAE

**变分自编码器（Variational Autoencoder）** 。

## Review

语音表示分为两种：连续表示和离散表示。

连续表示中在音频编辑领域突出的是 **VAE 潜向量** 和 **梅尔频谱** 。后者依赖独立组件。
（待补充）

离散表示中又分为 **声学token** 和 **语义token** 。声学token 中 **EnCodec** 广为人知。
（待补充）

早期的编辑模型多有涉及，不过逐渐采用了 **VAE** 。

## VAE

调用VAE的方式
（待补充）